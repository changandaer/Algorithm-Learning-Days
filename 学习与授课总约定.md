# AI算法实习学习与授课总约定

> 版本日期：2026-09-09。本文件独立于每天的课程。以后如果教学方向偏离，可以把本文件重新发给老师作为恢复上下文的依据。新增：LeetCode主线优先，Python面向对象至Transformer作为分阶段支线。

## 一、学习目标

当前最优先的目标不是看完最多课程，而是在理解原理的前提下，尽快获得以下能力：

1. 独立使用Python解决简单LeetCode题，并逐步进入常见中等题。
2. Python基础通过阶段验收后开始C++17；同一个算法思想能分别用Python和C++写出。
3. 能在企业机试和面试压力下闭卷写代码、检查边界并解释复杂度。
4. 用逐步变难的项目证明自己能分析问题、实现功能、调试错误和交付结果。
5. 最终沿共同基础进入PyTorch、Transformer和AI Infra训练系统，而不是停留在刷语法题。
6. 支线需要达到：理解Python的类、属性、方法、封装、组合与继承；随后将Transformer的数学公式与实现逐项对应，最终能从空白写出可训练、可验证的小型模型。

“16周”是目标周期，不用按时看完课程冒充学会，也不承诺覆盖所有公司的所有岗位。

## 二、岗位导向与学习取舍

根据`岗位介绍`文件夹现有岗位，课程按三层安排。

### 第一层：不同岗位共同需要，优先学习

- Python与C++编程；
- 数据结构与算法；
- Linux和Git；
- 函数设计、代码规范、测试与调试；
- 数据读取、清洗、统计和评估；
- 数学基础、逻辑分析、沟通和项目表达。

### 第二层：AI Infra主线，基础扎实后学习

- PyTorch训练流程；
- Transformer；
- GPU显存、混合精度与性能分析；
- DDP、FSDP和检查点恢复；
- 训练与推理系统的基本对照。

### 第三层：公司或岗位专项，结课后按投递方向选择

- 昇腾、CANN、MindSpeed、Alcore和NoC；
- vLLM、SGLang、TensorRT-LLM的深入开发；
- CUDA算子、编译器、芯片和硬件仿真；
- Kubernetes等暂时不是入门主线的工具。

不因为招聘信息中出现很多名词就同时学习。当前只走“算法与AI Infra共同基础”这一条路。

## 三、固定教学原则

1. 理解优先于背诵：能解释数据如何变化、为什么这样写、错误在哪里，比记住一段答案更重要。
2. 解决问题优先于学习形式：三步分析是思考工具，不为了填写模板而填写；不再要求单独写TODO清单。
3. 边做边学：当天理论必须尽快进入代码、测试或小项目，不能连续几天只看教程。
4. 每天只增加一个主要新概念；当天LeetCode和项目尽量复用这个概念。根据2026-09-09的新要求，可以安排每次约30分钟的面向对象/模型基础支线，支线日的算法题使用已有知识；不在同一天同时引入陌生算法、整套类体系和模型数学。
5. 新语法先讲后用：必做题中出现的新标准库、函数或语法，必须先说明用途、输入、输出、数据变化和常见错误。
6. 先自己实现，再参考开源项目；开源代码用于对照结构、测试和差异，不用于整段复制。
7. 评判重点是能否独立解决核心问题，不把空行、命名偏好或罕见边界当成当天是否掌握的主要依据。

## 四、每天的固定组成

每天根据前一天的真实表现动态调整，但保持相同结构：

1. 前一天验收：运行全部代码，阅读代码注释和`课后总结.md`，并用额外输入检查函数是否真正通用。
2. 示范答案：下一天的总课程文档给出前一天全部示范代码，并逐题对比学员实现。
3. 理论学习：详细讲一个主概念，包括人话解释、准确术语、数据变化、常见错误、复杂度和岗位用途。
4. 每日LeetCode：原则上每天至少一道与当前知识匹配的简单题；能力稳定后再加入中等题。
5. 独立代码：算法题和项目功能从接近空白的文件开始，由学员自己写三步分析、实现和测试。
6. 小项目：把当天知识放入岗位相关的连续项目，而不是只做互不相干的语法练习。
7. 最少反馈：只需保留代码、运行结果、真实疑问和必要心得，不用写冗长学习流水账。

如果当天内容明显过多，优先级依次为：核心理论理解 → 每日LeetCode → 核心项目功能 → 扩展练习。

面向对象支线占已有学习时间中的小段，不默认增加整套课时。C++刚入门时优先建立编译与基本语法能力，Python支线只短复习；通常每周安排2～3次支线，遇到主线超时可顺延。保留每天至少一道匹配当前水平的LeetCode；复习日可以重写旧题，不以每天必须完成一道陌生题为硬指标。

## 五、代码文件怎样提供

- 每天只有一个老师编写的总课程Markdown。
- Python或C++练习文件只保留简短题目说明，不预写函数实现、循环、变量、三步法、TODO和断言代码。
- 函数名称、输入输出要求、规定测试数据写在课程Markdown中，学员从空白手写。
- 简单题默认不给算法步骤；困难时才按“引导问题 → 伪代码 → 局部骨架 → 完整讲解”的顺序提供帮助。
- 得到完整答案后，应关闭答案，从空白重新写并运行测试。
- 不使用尚未教学的库或API布置必做题。

## 六、固定三步分析

三步法保持不变，不再更换名称或引入新的学习流程：

1. 用最小数据说清输入和输出。
2. 用自己的话拆出需要完成的代码阶段。
3. 决定程序需要保存什么状态，并边写边用小数据检查。

学员自己写三步分析。老师不会预填答案，也不要求另外重复写TODO。

## 七、LeetCode训练安排

### 当前Python阶段

- 每天至少一道简单题，优先覆盖数组、字符串、哈希、双指针和二分。
- 每题必须能解释输入输出、核心状态、边界、时间复杂度和额外空间复杂度。
- 先独立手写普通函数，再适应LeetCode的`class Solution`外壳。

### C++阶段

- Python基础验收后开始C++17和STL，不同时从零并行学习两门语言。
- 先理解一套共同算法，再隔天用另一种语言重写；不背两套思路。
- 逐步训练`vector`、`string`、`unordered_map`、栈、队列、链表和树。

### 反复练习

老师负责把核心题安排在首次完成后的第1天、第3天、第7天和第21天再次出现；学员不需要维护额外表格。复习时优先闭卷重写、口述和修改测试，而不是重新看完整答案。

## 八、每日评价标准

- 概念理解：25分
- 代码正确性：30分
- 独立分析与调试：20分
- 测试、边界与复杂度：15分
- Git记录与技术表达：10分

评价顺序：

1. 是否能用自己的话解释核心知识；
2. 是否能从空白独立实现核心代码；
3. 是否能运行并解释主要变量和结果；
4. 是否能用不同输入发现只对样例有效的代码；
5. 最后才是命名、格式和可读性建议。

80分以上且无核心误解时进入新知识；60～79分先补关键短板并加入少量新内容；60分以下或不能复现时不增加新主线。未教学内容造成的未完成不扣分。

## 九、项目与GitHub规则

- 项目从小到大连续生长：岗位数据处理 → C++日志分析 → 微型自动求导 → Transformer训练器 → 分布式训练实验。
- 参考GitHub项目之前必须先完成自己的基础版本。
- 只选择与当前知识直接相关的局部内容，对照函数边界、测试、README、目录结构和许可证。
- 项目必须记录自己解决的问题、错误定位、测试结果和与参考实现的差异。
- 最终只展示3～4个能运行、能解释、能复现的项目，不用大量半成品充数。
- 不上传教材、岗位截图、数据集副本、模型权重、密钥或没有许可的内容。

当前算法代码可在独立完成后与[TheAlgorithms/Python](https://github.com/TheAlgorithms/Python)对照；岗位技能项目只借鉴[Python-Word-Frequency](https://github.com/amanda-mcmullin/Python-Word-Frequency)的“读取—标准化—统计—输出”任务结构。更完整的[skill-extractor](https://github.com/dreamjobs-tech/skill-extractor)只作为以后理解真实工程输入输出和测试结构的参考，当前不安装、不照抄。

## 十、AI使用规则

- AI是解释、调试引导和代码审查助手，不代替第一次思考。
- 独立题先自己分析和尝试；卡住时应先提供自己的三步分析、代码、实际输出和报错。
- 不直接复制AI完整答案提交。
- 看到答案后必须能关闭答案重新写，并解释每行为什么存在。
- 周测、阶段验收和模拟机试关闭AI。

## 十一、文件与Git约定

- 仓库固定为`Algorithm-Learning-Days`。
- 每天只新增`DAY13`、`DAY14`等日期目录，不删除以前完成的日期目录。
- 每天目录包含一个课程总Markdown、必要的空白代码文件和学员自己写的课后总结。
- 不在每天目录重复创建求职准备、参考资料索引等通用结构。
- 不生成或提交`.DS_Store`；通过仓库根目录`.gitignore`统一忽略。
- 老师不会未经要求替学员提交或推送Git。

## 十二、接下来一段的实际推进

1. DAY13：文件读取与岗位数据输入；每日题为LeetCode 242“有效的字母异位词”。
2. DAY14：Python第一阶段综合验收；新题LC387，复习LC1和LC242，验证文件与函数；支线首次学习类、对象、实例属性和方法。
3. 通过DAY14后开始C++17；学习编译、变量、判断、循环、函数和`vector`，并把已掌握的简单题逐步改写为C++。
4. C++基础稳定后继续数据结构、常见算法、机器学习数学和PyTorch，不因为着急而同时开启多条陌生主线。

这是一条可根据每日结果调整的近期路径，不是要求机械追赶的僵硬日历。

## 十三、面向对象到Transformer的支线计划

这里是后续路线，列出的术语不要求今天背诵。每次授课只取一个相邻小节，并按“专业解释＋人话＋小例子＋独立作业”讲清楚。下一次生成课程时，应检查上一节支线练习和心得再推进。

| 阶段 | 学什么、解决什么问题 | 实践与验收 |
|---|---|---|
| A：对象基础，DAY14开始 | 类是定义，对象是具体实例；属性保存各自的数据，方法执行操作；理解`self`与`__init__` | 独立写一个计数器类；两个对象分别记录，互不影响；说明局部变量与实例属性的区别 |
| B：可靠的类设计，之后数次短课 | 实例属性与类属性、可变容器共享问题；封装是整理状态与操作；`_name`约定；组合、继承、方法重写和`super()` | 修复共享列表错误；组合两个小对象；实现一个简单父类与子类，解释初始化顺序；`@property`按需要后置讲解 |
| C：模型所需数学 | 向量、矩阵乘法、转置、张量形状；导数、链式法则；概率、softmax与交叉熵 | 手算小向量/矩阵和一次参数更新；每个公式指向对应代码，不以“看过公式”为通过 |
| D：模型代码组织 | 学完模块导入和张量后，学习`nn.Module`、`__init__`、`forward`、子层和参数；建立计算图、反向传播与优化器的联系 | 从空白写小型网络和CPU训练循环；区分模型长期参数与本次输入、中间结果；检查梯度、保存加载 |
| E：注意力与Transformer部件 | Q/K/V是从输入算出的三组表示；点积评分、缩放、softmax、掩码、加权求和；再学习多头、位置表示、残差、归一化和前馈网络 | 先手算2～3个词的小例子；手敲注意力和各部件，逐步解释形状、结果与梯度；能定位维度和掩码错误 |
| F：完整小模型 | 将部件组合成小型仅解码器模型，再补编码器、交叉注意力及编码器—解码器版Transformer | 手写数据输入、模型、损失、训练、保存加载和生成；用小数据验证训练与生成；区分两种架构，不能把一段注意力函数算作完整Transformer |

“完整手敲”指能依靠理解，从空白写出模型各部件及训练流程，并解释每个公式和张量尺寸。实现注意力时使用PyTorch基础张量运算，不以调用`nn.Transformer`或`nn.MultiheadAttention`代替所学实现；可以使用已理解的自动求导和基本层，不要求重写整个PyTorch或CUDA。

数学和代码配套验收：解释输入的B（同时处理多少条样本）、T（序列长度）、D（每个位置有多少个特征）；核对矩阵相乘尺寸；在关闭随机丢弃等条件下验证因果掩码不让未来词影响过去；比较参考数值；在固定小数据上验证损失下降及保存加载后输出一致。这些检查在教过相关概念后才布置。

近期安排：DAY14完成A的第一次练习；DAY15若进入C++，支线只复习对象和`self`；DAY16以后按掌握程度继续实例属性、封装和可变状态。进入模型阶段前必须补齐数学与PyTorch基础，不承诺几天内从类语法直接跳到完整Transformer。

开源参考分阶段选章：[Python官方类教程](https://docs.python.org/3.11/tutorial/classes.html)、[PyTorch构建网络教程](https://docs.pytorch.org/tutorials/beginner/basics/buildmodel_tutorial.html)，再到[LLMs-from-scratch](https://github.com/rasbt/LLMs-from-scratch)第3章注意力、第4章GPT、第5章训练。该仓库代码许可见[LICENSE.txt](https://github.com/rasbt/LLMs-from-scratch/blob/main/LICENSE.txt)，书籍与相关图片不按代码许可处理。只在自己实现后选读相应模块，不直接照抄整仓。

原始Transformer架构参考[Attention Is All You Need](https://arxiv.org/abs/1706.03762)。先完成便于逐步验证的小型仅解码器版本，随后补齐原始编码器—解码器结构，明确每个阶段实现范围。学习环境仍以Mac本地小实验为先。
