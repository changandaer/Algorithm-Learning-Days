"""DAY12热身：修复DAY11跟做文件中的检查顺序与测试结果。"""


# 固定三步法：
# 第一步：最小输入与输出：[3, 3], 6 -> [0, 1]
# 第二步：建空字典；逐个计算needed；先查找；没找到再记录
# 第三步：观察第一个3和第二个3分别执行了什么


def two_sum_hash(nums, target):
    # TODO：从空白手写正确的哈希解法。
    pass


# 完成函数后取消注释并运行：
# assert two_sum_hash([2, 7, 11, 15], 9) == [0, 1]
# assert two_sum_hash([3, 2, 4], 6) == [1, 2]
# assert two_sum_hash([3, 3], 6) == [0, 1]
# assert two_sum_hash([3], 6) == []

