1. 数据结构和算法有什么区别？是否必须先学完所有数据结构才能刷题？
数据结构是数据的组织和处理，算法是解决同一个问题的不同方式，每个方式的时间复杂度与空间复杂度都不同。不需要

2. 大O为什么不等于程序实际运行了多少秒？
Order of magnitude，复杂度，空间复杂度指的是在存储空间上的最坏情况，时间复杂度是指在处理时间上的趋势，大O符号用来描述算法的渐进上界。
简单来说，它衡量的是当输入数据规模（通常用 n 表示）趋向于无穷大时，算法的运行时间或占用空间的增长趋势和最坏情况。

3. 两个前后相接的n次循环为什么是O(n)，两个嵌套的n次循环为什么是O(n²)？
两个前后相接的n次循环是n+n = 2n，增长部分主要是n，所以O(2n)=O(n)
两个嵌套的n次循环是n*n，增长部分就是n*n，所以是O(n²)

4. `target in 列表`和`target in 字典`大致怎样寻找目标？
`target in 列表`通过索引由0到n一个接着一个寻找，`target in 字典`是通过哈希表计算的门牌号，直接去找门牌号就可以找到，如果有哈希冲突，也可以找门牌号隔壁的几个目标

5. 为什么字典查找只能说平均O(1)，而不保证永远O(1)？
因为有哈希冲突，最坏的结果是O(n)

6. 暴力两数之和为什么是O(1)额外空间，而哈希法是O(n)？
因为字典需要不断存储看到的数字，而暴力之需要记住每一次的索引数字即可

7. 用自己的话解释“哈希法用空间换时间”。
哈希法通过特有的计算方式计算出门牌号，将数据放入各个具有门牌号的空间内，寻找时只需要寻找门牌号，也就是用空间换时间

8. 不看代码，口述“判断重复元素”的哈希解法。
创立空字典，如果元素在字典内，就返回索引，如果不在就将元素添加到空字典中