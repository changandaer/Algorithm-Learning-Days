1. `found_index = -1` 和 `skills[-1]` 中的 -1 有什么不同？
`found_index = -1`的-1是哨兵值，意味还未找到，`skills[-1]`的-1代表skills这个列表的最后一个元素

2. 为什么两数之和的内层索引应从 `first_index + 1` 开始？
因为避免了同一个位置的索引值进行相加

3. 用人话解释“标准化必须处理比较双方”。
用同一套标准处理双方数据才能便于今后在同一个规则下对比

4. 统计多个技能时，为什么每换一个目标都要把 `current_count` 重新设为 0？
否则会导致`current_count`出现累加

5. 题目写“输出索引和数值”，怎样设计测试证明两项都完成？
更换不同的目标值成功输出就算完成

6. 三步拆题卡是否比六格法容易？哪一步仍让你不舒服？
三步更容易，三步的第一步拿两个小数据手算还是不舒服，感觉应该先把工作切分之后再写代码的过程中不断用小数据算，同时记录每个阶段程序要记住的东西

7. 你认为今天哪个错误最接近真实工作中的回归测试问题？为什么？
还没真正完成一个项目，我目前完成的都是基础的python题目，不算是回归测试

8. 我看你的计划是DAY09学习字典/哈希表，然后DAY10正式完成第一道 Hot 100？
难道不用学习python的更高级知识吗，比如：对象、类的成员方法、构造方法、魔术方法、封装、继承、复写、多态等等知识吗