1. 344为什么检查原列表，而不是只检查返回值？奇数长度时，中间一项怎样处理？
只检查返回值不能证明列表被正确修改，奇数长度，中间一项可以不用处理

2. C++循环的初始化、判断、循环体、更新按什么顺序发生？累计量为什么不能每轮重新归零？
第一步初始化，第二步判断，第三步执行循环体，第四步更新，累积量每轮归零就不是累积量，就只是最后一个量的值了

3. 用今天的猫、狗、车说明：为什么查表不等于学习？表里的值依据什么变好？换了预测器，原来的表为什么未必仍然合适？
查表只是输入，并不是学习。

4. 对网络A的x=−1，口述两个神经元各算了什么；指出模型参数、中间结果分别在哪里。今天有端到端训练吗，依据是什么？
两个神经元各自根据自己的weight与bias计算前向输出的数值，只是第二个神经元的输入是第一个神经元的输出，模型参数在w1,b1,w2,b2中，中间结果是h。今天没有端到端训练，依旧没有根据样本数据来优化模型参数。

今天的神经网络理论知识你根据我上一次的要求讲解，我大致了解了，但是代码实践题目我没有看懂你需要我做什么，下次把题目讲得更清楚一些。