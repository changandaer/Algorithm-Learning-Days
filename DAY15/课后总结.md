1. C++修改源码后为什么要重新编译？`main`中的`return 0`和输出0有什么不同？
C++是先complie生成可执行文件之后再运行，所以修改代码就得重新编译
`main`中的`return 0`是给电脑说程序运行无误，与输出0不同

2. 用自己的函数解释返回类型、参数类型；为什么耗时累加器要初始化为0？
3. 用`vector`和Python列表比较今日遍历写法；当前值传参为什么会有输入副本？
C++我以前从来没有接触过，python我之前是学习过的，所以今天第一天学习C++你就让我自己用vector和函数实现训练耗时统计，我觉得有难度有些大，并且在你的C++理论教学中你也并不是针对小白讲解的，比如为什么在C++的程序开头放的iostream、cassert的全称是什么含义是什么，比如你说`values.size()`可以读元素个数，`values.empty()`判断是否为空，但是你并没有用代码举例说明这些内容。综上，我认为你不能一味的求快，新的事物我刚开始学习需要逐步夯实基础，你可以通过我每天的反馈认为我学习的程度来调整我的学习速度，而不是第一天学习C++就直接让我写两个函数，我想要知其然也要知其所以然。

4. LC217最重要的状态是什么？它的空间分析为什么不能照搬LC387的“26种字母”？顺带用一句话修正“哈希值一定唯一”。
LC217最重要的状态是保存数字的字典，空间复杂度是有可能随着输入的列表长度增加而增加的，LC387的“26种字母”字典上线就是26，哈希值不一定唯一，有可能出现哈希冲突

5. 神经元的输入、参数、中间值、输出分别是什么？手算一组自己增加的数据。
输入是x，参数是权重weight和偏置bias，中间值是线性值x*weight+bias，输出值是经过ReLU的x*weight+bias

6. `self.weight`与输入x分别属于谁？创建两个实例为何需要分别测试？只调用forward为什么还不叫训练？
`self.weight`属于创立的实例对象，输入x来自实例对象
创建两个实例为何需要分别测试，不清楚
只调用forward为什么还不叫训练，模型训练是一个复杂的闭环过程，还需要反向传播等等